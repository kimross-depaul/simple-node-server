name: Pipeline Demo

on:
    push:
        branches:
            - "**"
            
jobs:
    hello-job:
        runs-on: ubuntu-latest
        steps:
            - name: Say hello
                run: echo "Yay Github actions is running!"
